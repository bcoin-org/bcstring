/* eslint-env mocha */
/* eslint prefer-arrow-callback: "off" */

'use strict';

const assert = require('./util/assert');
const { cashaddr } = require('../lib/binding');

const largevectors = require('./data/cashaddrlarge.json');
const legacyvectors = require('./data/legacyvectors.json');

describe('cashaddr native', function() {

});
